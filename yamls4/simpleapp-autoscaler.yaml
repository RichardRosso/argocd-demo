apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: simpleapp-autoscaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: simpleapp-deploy
  minReplicas: 40
  maxReplicas: 50
  targetCPUUtilizationPercentage: 80
